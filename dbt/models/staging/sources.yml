version: 2

sources:
  - name: public
    description: "Schema PostgreSQL alimente par load_to_db.py"
    tables:
      - name: raw_articles
        description: "Articles bruts collectes par acquisition.py"
        